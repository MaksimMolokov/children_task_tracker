# Спецификация проекта "Детский контроллер"

## 1. Цель проекта

Реализовать Telegram-бота "детский контроллер", который:

1. Автоматически **выдаёт детям задания** по расписанию (Duolingo, чтение, физ. упражнения, учиру и т.д.).
2. Напоминает каждому ребёнку о задачах в нужное время.
3. Принимает подтверждение выполнения (кнопка + медиа).
4. Автоматически **закрывает задачи в конце дня** (выполнено / не выполнено).
5. Считает **деньги по индивидуальным ставкам** за каждое задание для каждого ребёнка.
6. Шлёт **ежедневные и еженедельные отчёты** админу.

Цель: максимально снять с родителя ручной контроль — бот сам напоминает, сам собирает "сделано", сам считает, сам присылает отчёты.

---

## 2. Стек и архитектура

### 2.1. Технологии

* Язык: **Python 3.11+**
* Telegram: **aiogram** (v3)
* БД: **PostgreSQL**
* Планировщик: **APScheduler** (внутри бота) или cron-джобы, вызывающие сервисные методы.
* Развёртывание: **Docker** (минимум 2 контейнера: bot + postgres).

### 2.2. Компоненты

1. **Bot service**
   * Получает апдейты от Telegram.
   * Обрабатывает команды админа и детей.
   * Создаёт TaskInstance по расписанию.
   * Принимает подтверждения выполнения и медиа.
   * Формирует и отправляет отчёты.

2. **Database**
   * Хранит пользователей, задания, расписания, инстансы задач, медиа, настройки.

3. **Scheduler**
   * Периодически (по конфигу) запускает:
     * создание ежедневных задач,
     * закрытие "просроченных" задач в конце дня,
     * отправку ежедневных отчётов,
     * отправку еженедельных отчётов.

---

## 3. Сущности и модель данных

Ниже — логическая модель. В Cursor/миграциях можно оформить как SQL/SQLAlchemy models.

### 3.1. Пользователь

**Table `users`**

* `id` (PK)
* `telegram_user_id` (BIGINT, unique)
* `role` (ENUM: `admin`, `child`)
* `display_name` (TEXT) — как будет называться ребёнок в отчётах: "Иван", "Роман".
* `created_at` (TIMESTAMP)
* `is_active` (BOOLEAN, default true)

### 3.2. Чаты и канал

**Table `chats`**

* `id` (PK)
* `telegram_chat_id` (BIGINT, unique)
* `type` (ENUM: `family_group`, `admin_private`, `public_channel`)
* `description` (TEXT, optional)
* `created_at` (TIMESTAMP)

Привязки:

* Админский приватный чат можно не хранить отдельно, а считать по `telegram_user_id` админа.
* `family_group` — устаревший тип, не используется для отправки заданий.
* `public_channel` — канал, куда бот может шлёт публичные отчёты (опционально).

### 3.3. Тип задания (шаблон)

**Table `task_types`**

* `id` (PK)
* `name` (TEXT) — "Чтение 30 минут", "Duolingo 1 урок".
* `description` (TEXT)
* `category` (TEXT или ENUM: `reading`, `language`, `sport`, `study`, `other`)
* `requires_media` (BOOLEAN) — нужно ли обязательное медиа.
* `is_active` (BOOLEAN)
* `created_at` (TIMESTAMP)

### 3.4. Ставки (денежные коэффициенты) для ребёнка

**Table `child_task_rewards`**

* `id` (PK)
* `child_id` (FK -> users.id, role=child)
* `task_type_id` (FK -> task_types.id)
* `reward_amount` (NUMERIC) — сумма за 1 выполненное задание.
* `currency` (TEXT, default `ARS`)
* `created_at` (TIMESTAMP)

> При создании TaskInstance значение `reward_amount` берётся из этой таблицы и записывается в TaskInstance как **снапшот**.

### 3.5. Расписание

**Table `schedules`**

* `id` (PK)
* `task_type_id` (FK -> task_types.id)
* `periodicity` (ENUM: `daily`, `weekly`)
* `time_of_day` (TIME) — локальное время.
* `days_of_week` (TEXT) — строка, напр. `"MON,TUE,WED,THU,FRI,SAT,SUN"` или `"MON,WED,FRI"`.
* `target_scope` (ENUM: `all_children`, `specific_children`)
* `target_children_ids` (TEXT, JSONB array of child_ids, if `specific_children`)
* `is_active` (BOOLEAN)
* `created_at` (TIMESTAMP)

### 3.6. Инстанс задания (конкретный день/ребёнок)

**Table `tasks`**

* `id` (PK)
* `child_id` (FK -> users.id)
* `task_type_id` (FK -> task_types.id)
* `scheduled_date` (DATE) — на какой день задание.
* `status` (ENUM: `pending`, `done`, `failed`, `expired`)
* `reward_amount` (NUMERIC) — **фиксированная сумма**, скопированная из `child_task_rewards.reward_amount` на момент создания.
* `currency` (TEXT)
* `created_at` (TIMESTAMP)
* `completed_at` (TIMESTAMP, nullable)
* `message_id` (BIGINT, nullable) — ID сообщения с заданием в чате/личке.
* `chat_id` (BIGINT, nullable) — в каком чате отправлено (для reply и кнопок).

### 3.7. Медиа по заданиям

**Table `task_media`**

* `id` (PK)
* `task_id` (FK -> tasks.id)
* `telegram_file_id` (TEXT)
* `file_type` (ENUM: `photo`, `video`, `document`)
* `uploaded_at` (TIMESTAMP)

---

## 4. Логика и поведение

### 4.1. Регистрация и роли

1. В конфиге задаётся `ADMIN_TELEGRAM_ID` (или секретный код).
2. При `/start` от этого пользователя бот помечает/подтверждает его как `admin`.
3. Для добавления ребёнка:
   * Команда админа: `/add_child`
   * Бот спрашивает: "Как зовут ребёнка?" → сохраняет `display_name`.
   * Бот просит: "Перешлите или ответьте на любое сообщение от ребёнка, чтобы привязать его аккаунт" → читает `from.id` → создаёт `users` с ролью `child`.
4. Бот работает в:
   * личке с админом (команды),
   * личке с детьми (напоминания и задания).

### 4.2. Управление типами заданий

Команды только для админа (по `telegram_user_id`):

* `/add_task_type`
  * Бот ведёт диалог:
    1. Название.
    2. Краткое описание.
    3. Категория.
    4. Требуется ли медиа (да/нет).
  * Создаёт запись в `task_types`.

* `/list_task_types`
  * Список активных шаблонов с ID-названиями.

* `/disable_task_type <id>`
  * Помечает `is_active = false`.

### 4.3. Настройка денежной ставки

* `/set_reward`
  * Диалог:
    1. Выбор ребёнка (список по `users.role=child`).
    2. Выбор задания (список `task_types`).
    3. Ввод суммы.
  * Создаёт/обновляет запись в `child_task_rewards`.

* `/list_rewards [child_name]`
  * Показать все пары "задание → ставка" для ребёнка.

Правило: если для конкретного child+task_type нет записи в `child_task_rewards`, то `reward_amount` считается 0 (задание можно выполнить, но денег не даёт).

### 4.4. Настройка расписания

* `/add_schedule`
  * Диалог:
    1. Выбор TaskType.
    2. Выбор детей:
       * "Все дети" или "Выбрать вручную".
    3. Периодичность:
       * `ежедневно`,
       * `конкретные дни недели` (бот предлагает чекбоксы: Пн–Вс).
    4. Время отправки (HH:MM).
  * Создаёт запись в `schedules`.

* `/list_schedules`
  * Показывает все активные расписания: task_type, время, дни недели, целевая группа.

* `/disable_schedule <id>`
  * `is_active = false`.

---

## 5. Выдача задач и подтверждение выполнения

### 5.1. Создание задач по расписанию (scheduler job)

Ежеминутно/ежепять минут планировщик проверяет:

1. Выбирает все активные `schedules`, у которых `time_of_day` == текущему локальному времени (с учётом таймзоны) и сегодня входит в `days_of_week`.

2. Для каждого такого расписания и для каждого целевого ребёнка:
   * Проверяет, нет ли уже `tasks` на **сегодня** (`scheduled_date=today`, `child_id`, `task_type_id`) в статусах `pending` или `done`.
   * Если нет, создаёт новую запись в `tasks`:
     * `reward_amount` и `currency` берутся из `child_task_rewards`. Если нет записи — 0.

3. Отправляет ребёнку сообщение в личные сообщения:
   * Текст, например:
     > Иван, твоё задание на сегодня:
     > 📖 Чтение 30 минут
     > Награда: 1000 ARS
     > Когда выполнишь — нажми кнопку ниже и (если нужно) пришли фото/видео **ответом на это сообщение**.
   * Inline-кнопка: `✅ Выполнил` (callback).

4. В `tasks` сохраняет `chat_id` и `message_id` этого сообщения — для последующего match reply и callback.

### 5.2. Обработка нажатия "✅ Выполнил"

При приходе callback от inline-кнопки:

1. По `callback.message.chat.id` и `callback.message.message_id` находим `task` (по связке `chat_id` + `message_id`).
2. Проверяем:
   * `status` должен быть `pending`.
   * `scheduled_date` не позже сегодняшнего дня (нет смысла делать за прошлый день, если решили так).
3. Далее:
   * Если `requires_media = false`:
     * Ставим `status = done`, `completed_at = now`.
     * Обновляем текст сообщения (по возможности) или шлём отдельное: "Отлично, задание засчитано 👍".
   * Если `requires_media = true`:
     * Ставим флаг в сессии/кэше, что **следующее медиа-сообщение** от этого ребёнка в этом чате должно привязаться к данному `task.id`.
     * Отвечаем: "Пришли, пожалуйста, фото/видео ответом на это сообщение, чтобы я засчитал задание."

### 5.3. Обработка медиа

Когда бот получает от ребёнка фото/видео/документ:

1. Проверяет, есть ли в сессии ожидание медиа для этого ребёнка и чата.
2. Дополнительно: можно требовать, чтобы это было `reply` на сообщение бота с заданием (проверить `message.reply_to_message.message_id` → `tasks.message_id`).
3. Если всё ок:
   * Сохраняем `task_media` с `telegram_file_id`.
   * Обновляем `tasks.status = done`, `completed_at = now`.
   * Отвечаем ребёнку: "Супер, задание засчитано 💪".

---

## 6. Автоматическое закрытие задач и отчёты

### 6.1. Закрытие задач в конце дня

Ночной/поздний job, каждый день, напр. в 23:59 или 00:10.

Шаги:

1. Берём все `tasks` с:
   * `scheduled_date = вчера` (или сегодня, зависит от политики),
   * `status = pending`.
2. Ставим им `status = failed` или `expired` (одно из значений, лучше `failed`).
3. Эти задачи попадают в отчёт как невыполненные.

### 6.2. Ежедневный отчёт админу

Job, каждый день утром, напр. в 08:00.

1. Диапазон отчёта: **вчерашний день** (`report_date = today - 1`).

2. Достаём все `tasks` за этот день.

3. Группируем по `child_id` и `task_type_id`.

4. Считаем для каждого задания:
   * статус,
   * `reward_amount` (0, если не выполнено),
   * суммарную сумму по ребёнку за день.

5. Формируем текст:
   > Отчёт за 2025-12-10
   >
   > 👦 Иван
   > – Чтение 30 минут — ✅ (1000 ARS)
   > – Duolingo 1 урок — ❌ (0 ARS)
   > Итого за день: 1000 ARS
   >
   > 👦 Роман
   > …

6. Отправляем **только админу** (в личку).

### 6.3. Еженедельный отчёт

Job, раз в неделю, напр. воскресенье 20:00.

1. Диапазон: `week_start = Monday`, `week_end = Sunday` (последние 7 дней).

2. Достаём все `tasks` за этот период, `status = done`.

3. Группировка по `child_id` + `task_type_id`.

4. Считаем:
   * количество выполненных заданий,
   * суммарный заработок по каждому типу,
   * общую сумму по ребёнку.

5. Формируем текст:
   > Еженедельный отчёт (2025-12-01 — 2025-12-07)
   >
   > 👦 Иван
   > – Чтение 30 минут: 5 шт (5 000 ARS)
   > – Duolingo: 6 шт (3 000 ARS)
   > Итого: 8 000 ARS
   > …

6. Отправляем админу.
   (Опционально — отдельная настройка для отправки сокращённой версии в канал/чат, это можно оставить на второй этап.)

---

## 7. Админ-меню и кнопки бота

### 7.1. Точка входа в админку

* Команда: `/admin`
* Доступна только пользователю с ролью `admin`.
* Ответ на `/admin` — главное админ-меню (inline-клавиатура):

**Главное админ-меню (inline-кнопки):**

1. 📋 Проверка заданий → `ADMIN_CHECK_TASKS`
2. ➕ Назначить задание → `ADMIN_ASSIGN_TASK`
3. 🗓 Расписания → `ADMIN_SCHEDULES`
4. 👦 Дети → `ADMIN_CHILDREN`
5. 💰 Ставки → `ADMIN_REWARDS`
6. 📊 Отчёты → `ADMIN_REPORTS`
7. 🏆 Лидеры → `ADMIN_LEADERS`

Это один экран, можно делать 2–3 кнопки в ряд.

### 7.2. Блок "📋 Проверка заданий"

**Задача:** быстро посмотреть, что дети сдали, что нет, и при необходимости вручную принять/отклонить.

При нажатии **📋 Проверка заданий** (callback `ADMIN_CHECK_TASKS`):

Бот показывает подменю:

1. Сегодня по детям → `ADMIN_CHECK_TODAY_BY_CHILD`
2. По ребёнку → `ADMIN_CHECK_BY_CHILD_SELECT`
3. Требуют проверки (если нужна ручная модерация медиа) → `ADMIN_CHECK_NEED_REVIEW`
4. Просроченные / не сделаны → `ADMIN_CHECK_FAILED_TODAY`
5. ⬅️ Назад → возвращает в главное админ-меню.

#### 7.2.1. Сегодня по детям (`ADMIN_CHECK_TODAY_BY_CHILD`)

* Бот собирает все `tasks` за сегодня по каждому ребёнку.
* Отправляет компактный список:

> Задания на сегодня (2025-12-07)
>
> 👦 Иван
> – Чтение 30 минут — ✅ / ❌
> – Duolingo — ⏳ (ещё не отметили)
>
> 👦 Роман
> – Приседания — ✅
> …

Опционально под этим сообщением кнопки:

* Обновить → `ADMIN_REFRESH_TODAY`
* Перейти к модерации → `ADMIN_CHECK_NEED_REVIEW`

#### 7.2.2. По ребёнку (`ADMIN_CHECK_BY_CHILD_SELECT`)

Шаги:

1. Бот выводит список детей кнопками:
   * 👦 Иван → `ADMIN_CHECK_CHILD:<child_id>`
   * 👦 Роман → `ADMIN_CHECK_CHILD:<child_id>`

2. При выборе ребёнка:
   * Показывает за сегодня или за выбранный день его задачи
   * Кнопки: ◀️ День назад, Сегодня, День вперёд, ⬅️ Назад

В карточке ребёнка:

> Иван — 2025-12-07
> – Чтение 30 минут — ✅ (1000 ARS)
> – Duolingo — ⏳
> – Приседания — ❌

Для каждой строки можно сделать кнопку "Открыть задание":
* Открыть задание → `ADMIN_OPEN_TASK:<task_id>`

#### 7.2.3. Просмотр отдельного задания (`ADMIN_OPEN_TASK:<task_id>`)

Бот показывает подробности:

> Задание #12345
> Ребёнок: Иван
> Тип: Чтение 30 минут
> Дата: 2025-12-07
> Статус: ⏳ pending
> Награда: 1000 ARS

Медиа, если есть, можно получить по `task_media`.

Кнопки:

* ✅ Принять → `ADMIN_TASK_SET_DONE:<task_id>`
* ❌ Отметить как не выполнено → `ADMIN_TASK_SET_FAILED:<task_id>`
* 🔁 Сбросить на "ожидает" → `ADMIN_TASK_SET_PENDING:<task_id>`
* ⬅️ Назад → возвращает в список ребёнка / "сегодня по детям"

Это и есть ответ на "админ мог сдавать / принимать задания" — вручную управлять статусом.

#### 7.2.4. Требуют проверки (`ADMIN_CHECK_NEED_REVIEW`)

Если вводим механику модерации медиа (когда надо глазками посмотреть):

* Выбираем задачи, у которых:
  * `status = done`, `requires_media = true`, есть `task_media`,
  * но `is_reviewed = false` (можно добавить поле в `tasks`).
* Показываем по одной карточке:

> Иван, Чтение, 2025-12-07
> Прислано фото/видео.

Кнопки:

* ✅ Подтвердить → `ADMIN_REVIEW_APPROVE:<task_id>` (ставим `is_reviewed=true`)
* ❌ Отклонить (вернуть в pending) → `ADMIN_REVIEW_REJECT:<task_id>`
* Следующее → `ADMIN_REVIEW_NEXT`

### 7.3. Блок "➕ Назначить задание" (ручная выдача)

**Цель:** админ может разово выдать задание вне расписания (например, "сегодня сделать дополнительное чтение" или "супер-задача на выходные").

При нажатии **➕ Назначить задание** (`ADMIN_ASSIGN_TASK`):

Пошаговый диалог через inline-кнопки:

1. **Выбор ребёнка:**
   * Все дети → `ADMIN_ASSIGN_TASK_ALL_CHILDREN`
   * Выбрать ребёнка → список детей `ADMIN_ASSIGN_TASK_CHILD:<child_id>`

2. **Выбор типа задания:**
   * Список `task_types` (только `is_active = true`):
   * Чтение 30 минут → `ADMIN_ASSIGN_TASK_TYPE:<task_type_id>`
   * Duolingo → …

3. **Выбор даты:**
   * По умолчанию "Сегодня":
   * Кнопки: Сегодня, Завтра, Выбрать дату.
   * Для MVP можно ограничиться Сегодня и Завтра.

4. **Подтверждение:**
   * Бот показывает резюме:
   
   > Назначить задание:
   > Тип: Чтение 30 минут
   > Кому: Иван, Роман
   > Дата: 2025-12-07
   
   * Кнопки:
   * ✅ Назначить → `ADMIN_ASSIGN_TASK_CONFIRM`
   * ❌ Отмена

После подтверждения бот создаёт записи в `tasks` и сразу рассылает детям сообщения-напоминания, как при расписании.

### 7.4. Блок "🗓 Расписания"

Управление автоматической выдачей.

При нажатии **🗓 Расписания** (`ADMIN_SCHEDULES`):

Кнопки:

1. ➕ Новое расписание → `ADMIN_SCHEDULE_ADD`
2. 📃 Список расписаний → `ADMIN_SCHEDULE_LIST`
3. ⬅️ Назад

#### 7.4.1. Новое расписание (`ADMIN_SCHEDULE_ADD`)

Шаги (как мы описывали раньше, но в виде кнопок):

1. Выбор `task_type`.
2. Выбор целевых детей: Все / Выбрать.
3. Выбор периодичности:
   * Ежедневно (все дни),
   * Будни (пон–пт),
   * Выбрать дни недели (MON–SUN чекбоксами).
4. Выбор времени (список предопределённых времен: 08:00, 18:00, 20:00, или вводом).

Подтверждение → создаём `schedule`.

#### 7.4.2. Список расписаний (`ADMIN_SCHEDULE_LIST`)

* Список строк:
  * #1 Чтение 30 минут, каждый день, 20:00, все дети
  * #2 Duolingo, будни, 18:00, Иван, Роман

Для каждого:

* ✏️ Изменить (опционально в будущем)
* 🚫 Выключить → `ADMIN_SCHEDULE_DISABLE:<id>`

### 7.5. Блок "👦 Дети"

При нажатии **👦 Дети** (`ADMIN_CHILDREN`):

Кнопки:

1. ➕ Добавить ребёнка → `ADMIN_CHILD_ADD`
2. 📃 Список детей → `ADMIN_CHILD_LIST`
3. ⬅️ Назад

#### 7.5.1. Список детей (`ADMIN_CHILD_LIST`)

* Показываем список:
  * 👦 Иван (active)
  * 👦 Роман (active)

Для каждого:

* 🧾 Ставки → быстрый переход в `ADMIN_REWARDS_CHILD:<child_id>`
* 🚫 Отключить / Активировать → toggle `is_active`.

(Добавление ребёнка лучше сделать через команду/инструкцию, потому что нужно переслать его сообщение; здесь можно просто вывести подсказку).

### 7.6. Блок "💰 Ставки" (коэффициенты в деньгах)

При нажатии **💰 Ставки** (`ADMIN_REWARDS`):

Кнопки:

1. По ребёнку → `ADMIN_REWARDS_BY_CHILD_SELECT`
2. По заданию → `ADMIN_REWARDS_BY_TASKTYPE_SELECT`
3. ⬅️ Назад

#### 7.6.1. По ребёнку (`ADMIN_REWARDS_BY_CHILD_SELECT`)

1. Выбор ребёнка.
2. Показываем таблицу: "задание → текущая ставка".
3. Кнопки для каждой строки:
   * ✏️ Изменить → диалог ввода новой суммы `reward_amount`.
4. Кнопка ➕ Добавить ставку (выбрать `task_type`, задать сумму).

### 7.7. Блок "📊 Отчёты"

При нажатии **📊 Отчёты** (`ADMIN_REPORTS`):

Кнопки:

1. Отчёт за сегодня → `ADMIN_REPORT_TODAY`
2. Отчёт за вчера → `ADMIN_REPORT_YESTERDAY`
3. Еженедельный отчёт → `ADMIN_REPORT_WEEK`
4. (опционально) Выбрать период → `ADMIN_REPORT_CUSTOM`
5. ⬅️ Назад

Каждый отчёт:

* Формируется из таблицы `tasks` (как уже описано),
* Отправляется в том же чате с админом,
* Можно прикрутить кнопку 📥 Экспорт CSV в будущем.

### 7.8. Блок "🏆 Лидеры"

То, что ты называешь "список лидеров, кому задания выдаются" + классический лидерборд.

При нажатии **🏆 Лидеры** (`ADMIN_LEADERS`):

Кнопки:

1. За неделю → `ADMIN_LEADERS_WEEK`
2. За месяц → `ADMIN_LEADERS_MONTH`
3. ⬅️ Назад

Вывод:

> Лидерборд за неделю (2025-12-01 — 2025-12-07)
> 1. Иван — 9 500 ARS
> 2. Роман — 8 000 ARS

Можно отдельно добавить кнопку:

* 📤 Поделиться в канал → бот отправит это же сообщение в привязанный канал (если настроен).

### 7.9. Гайд для администратора по выдаче заданий

При первом запуске `/start` или `/admin` админ должен получить подробную инструкцию:

> **📖 Гайд по работе с ботом**
>
> **1. Настройка бота**
> 
> Для того, чтобы бот мог выдавать задания детям:
> 1. Детям нужно начать диалог с ботом (отправить /start) для регистрации
> 2. После этого бот сможет отправлять задания в личные сообщения
>
> **2. Как выдаются задания**
>
> Задания могут выдаваться двумя способами:
> 
> **А) Автоматически по расписанию:**
> - Зайдите в меню 🗓 Расписания → ➕ Новое расписание
> - Выберите тип задания, время, дни недели и детей
> - Бот будет автоматически создавать задания и отправлять их детям в личные сообщения
>
> **Б) Вручную через админ-меню:**
> - Зайдите в ➕ Назначить задание
> - Выберите ребёнка, тип задания и дату
> - Задание будет создано и отправлено немедленно в личные сообщения ребёнка
>
> **3. Где видны задания**
>
> - Все задания отправляются детям в личные сообщения
> - Вы можете проверить статус всех заданий через 📋 Проверка заданий
>
> **4. Проверка выполненных заданий**
>
> - Дети нажимают кнопку "✅ Выполнил" на задании
> - Если требуется медиа, дети отправляют фото/видео ответом на сообщение с заданием
> - Вы можете вручную проверить и принять/отклонить задания через 📋 Проверка заданий

---

